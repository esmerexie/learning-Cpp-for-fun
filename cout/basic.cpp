// essentialy the input and output needed for the conosle and std::cout to run
#include <iostream>

// every c++ program needs a main function. Everything inside main will be executed
int main()
{

// cout = character output.
// << = sends letters or numbers to the console to be output. 
    std::cout << "Hello Stranger!";

// all this return does is tells the computer that the program ran successfully if it returns 0
    return 0;
}
